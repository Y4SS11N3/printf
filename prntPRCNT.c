#include "main.h"

/**
 * prntPrcnt - Prints the percent symbol (%).
 * Return: Always returns 1, representing one character printed.
 */
int prntPrcnt(void)
{
	/* 'prcnt' is the ASCII value of the character '%' */
	int prcnt = 37;

	_putchar(prcnt);
	return (1);
}
